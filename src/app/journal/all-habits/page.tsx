import HabitForm from '@/components/HabitForm';
import React from 'react';

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <HabitForm />
    </div>
  );
};

export default page;
